# LEARNINGS — {{PROJECT_NAME}}

Technical journal for {{PROJECT_NAME}}. Future readers (humans + LLM sessions): this is the playbook, the traps, and the reasoning. Read once, save hours.

## Project goal

{{PROJECT_GOAL_PARAGRAPH}}

(Fill me in: one paragraph describing the project's scope, target environment, success criteria, and explicit non-goals. Keep it concrete — what does "done" look like?)

## Methodology that worked

(Fill me in: the high-level approach that actually shipped results. Order matters — describe the sequence of phases, what each phase accomplishes, and why earlier phases must precede later ones.)

## Things that did NOT work and why

(Fill me in: every failed attempt that cost meaningful time. For each: what was tried, what failed, root cause, and the pivot that unblocked progress. This section pays for itself the first time it prevents a repeat.)

## Common patterns and their fixes

(Fill me in: recurring error signatures, symptoms, or anti-patterns paired with their canonical fix. Format as a list keyed by the user-visible symptom so future-you can grep by error message.)

## Reproducible playbook

(Fill me in: numbered, copy-pasteable checklist for re-running the core workflow from scratch. Each step should be a single concrete action; assume the reader has a fresh checkout and a configured toolchain.)

## Toolchain setup

(Fill me in: exact install commands, version pins, env vars, and paths. Note any traps — formulas that need sudo, casks that fail non-interactively, version mismatches that silently break things.)

## Constraints and gotchas

(Fill me in: platform-specific oddities, shell quirks, API quotas, undocumented behaviour. Anything that bit you once and would bite again if forgotten.)
